execution order:

./makeCA
./makeCApubcertSS
./genServerPrivKey
./gentempCSRforServer
./signCSRwiththeCA

Output Files Identified

Here’s what you’ve got:

    CA Files:
        ca.key: CA’s private key (secret, keep safe).
        ca.crt: CA’s public certificate (share with clients like the CLI).
    Server Files:
        server.key: Server’s private key (secret, pass to server with --key).
        server.crt: Server’s public certificate (signed by ca.crt, pass to server with --cert).
        server.csr: Temporary CSR (can delete after signing).
        ca.srl: Serial number file (optional, for CA tracking).

